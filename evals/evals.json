{
  "skill_name": "aerospike-ce-ecosystem",
  "evals": [
    {
      "id": 1,
      "prompt": "Deploy a single-node Aerospike cluster for development on a kind cluster",
      "expected_output": "kubectl get asc shows phase=Completed, 1 pod Running",
      "files": []
    },
    {
      "id": 2,
      "prompt": "Deploy a 3-node Aerospike cluster with persistent storage in the aerospike namespace",
      "expected_output": "3 pods Running, PVC bound, phase=Completed",
      "files": []
    },
    {
      "id": 3,
      "prompt": "Scale the Aerospike cluster from 1 node to 3 nodes and dynamically change proto-fd-max to 20000",
      "expected_output": "scale complete, dynamic config applied",
      "files": []
    },
    {
      "id": 4,
      "prompt": "Create a FastAPI app that connects to Aerospike and performs CRUD on user records. Connect to localhost:3000",
      "expected_output": "App starts and CRUD operations work via curl",
      "files": []
    },
    {
      "id": 5,
      "prompt": "Add a batch read endpoint, an expression filter endpoint, and Prometheus metrics to the FastAPI app",
      "expected_output": "All endpoints work, /metrics exposed",
      "files": []
    },
    {
      "id": 6,
      "prompt": "Add proper error handling to the FastAPI app: 404 for not found, 409 for duplicates, 503 for connection issues",
      "expected_output": "Correct HTTP status codes returned",
      "files": []
    },
    {
      "id": 7,
      "prompt": "An Aerospike cluster has fallen into phase=ConfigDegraded. The dynamic config rollback appears to have failed -- walk me through recovery",
      "expected_output": "Inspect the DynamicConfigDegraded condition and status.pods[*].dynamicConfigChanges, explain that reconciliation is halted (ConfigDegradedSkip warnings every ~60s) until manual intervention, and advise the user NOT to toggle enableDynamicConfigUpdate but to revert the offending spec value to a known-good one, then cold-restart the pods / reset the phase so reconciliation resumes",
      "files": []
    },
    {
      "id": 8,
      "prompt": "Implement a FastAPI readiness probe using client.ping() and add an endpoint that creates multiple records via batch_write. If any record is in_doubt, signal 503 so the client reconciles state",
      "expected_output": "/health/ready uses await client.ping(); /records:batchWrite calls client.batch_write(...); inspects BatchRecord.in_doubt and returns 503 when any in_doubt items are present, else a normal response",
      "files": []
    },
    {
      "id": 9,
      "prompt": "Set up two ackoctl contexts: dev pointing at http://localhost:8000/api with workspace=default, and prod-us pointing at https://acm.prod-us.example.com/api with bearer token 'tk_prod_xxx' and workspace=prod-us. Make prod-us the current context and verify both are listed.",
      "expected_output": "ackoctl skill triggers; runs ackoctl config set-context dev --server=http://localhost:8000/api --workspace-id=default; runs ackoctl config set-context prod-us --server=https://acm.prod-us.example.com/api --token=tk_prod_xxx --workspace-id=prod-us; runs ackoctl config use-context prod-us; runs ackoctl config view -o yaml (or current-context) to confirm both dev and prod-us contexts are present and prod-us is the active context",
      "files": []
    },
    {
      "id": 10,
      "prompt": "I have ackoctl set up against my prod cluster (context prod-us). Cluster prod is reporting CrashLoopBackOff on pods. Walk me through diagnosis using ackoctl where available.",
      "expected_output": "acko-debugging skill triggers (was the acko-cluster-debugger agent before its demotion to a skill); uses ackoctl --context=prod-us cluster info / ackoctl info for the data plane and ackoctl --context=prod-us k8s cluster list / k8s cluster get / k8s cluster logs / k8s cluster events for the K8s plane. Falls back to kubectl only when ackoctl is unavailable or cluster-manager was started without K8S_MANAGEMENT_ENABLED=true (ackoctl k8s commands return 404). Never invokes destructive verbs (record put/delete, cluster configure-namespace, info --allow-write, index create/delete, k8s cluster scale, k8s cluster reconcile, udf upload/remove, admin user/role mutations, connection delete) without explicit user confirmation",
      "files": []
    },
    {
      "id": 11,
      "prompt": "My ACKO operator pod keeps CrashLooping after I patched the AerospikeCluster CR. Where do I file this bug?",
      "expected_output": "bug-reporter skill triggers; routes to aerospike-ce-kubernetes-operator repo (https://github.com/aerospike-ce-ecosystem/aerospike-ce-kubernetes-operator/issues/new); asks for the AerospikeCluster CR YAML (sanitized), kubectl describe asc Events, operator log slice, phase/conditions, and ACKO image version; suggests duplicate search via gh issue list --search before filing",
      "files": []
    },
    {
      "id": 12,
      "prompt": "aerospike_py에서 batch_write 호출하니 Rust panic이 났어. 어디다 제보해야 해?",
      "expected_output": "bug-reporter skill triggers; routes to aerospike-py repo (https://github.com/aerospike-ce-ecosystem/aerospike-py/issues/new); asks for python --version, pip show aerospike-py, the 5-20 line reproducer, the full traceback, and RUST_BACKTRACE=1 output for the panic",
      "files": []
    },
    {
      "id": 13,
      "prompt": "cluster-manager UI에서 Record browser 페이지가 500을 내는데 어디 repo에 이슈 올려야 돼?",
      "expected_output": "bug-reporter skill triggers; routes to aerospike-cluster-manager repo (https://github.com/aerospike-ce-ecosystem/aerospike-cluster-manager/issues/new); asks for browser+version, the failing URL path, browser console error, failing Network tab request/response, and screenshot or /api/v1/version output",
      "files": []
    },
    {
      "id": 14,
      "prompt": "ackoctl로 test 네임스페이스 users set에 임시 레코드 하나 넣어줘.",
      "expected_output": "ackoctl skill triggers; recognizes a data-plane write; runs ackoctl guide get data-plane FIRST to read the workspace operational policy before any record put; follows the policy it returns (e.g. caps test-data TTL at 7 days, attaches a note using the creator/date template); then runs ackoctl record put with the policy-compliant flags; if the data-plane guide returns 404 it tells the user no guide is registered and proceeds with standard caution rather than inventing a policy",
      "files": []
    }
  ]
}
